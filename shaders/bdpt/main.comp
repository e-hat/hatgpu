#version 460 core

layout(local_size_x = 1, local_size_y = 1) in;

layout (set = 0, binding = 1, rgba8) uniform image2D canvasImage;

void main() {
  imageStore(canvasImage, ivec2(gl_GlobalInvocationID.xy), vec4(0.0, 1.0, 0.0, 1.0));
}
